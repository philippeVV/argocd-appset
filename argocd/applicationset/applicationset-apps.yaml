apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps
  namespace: ops
spec:
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  argocd.argoproj.io/secret-type: cluster
          - git:
              repoURL: https://github.com/philippeVV/argocd-appset.git
              revision: HEAD
              directories:
              - path: 'tenants/**/*'
  template:
    metadata:
      name: '{{name}}-{{path.basename}}' # 'name' field of the Secret
    spec:
      project: "default"
      source:
        chart: 'https://github.com/philippeVV/nginx-helmchart'
        repoURL: https://github.com/philippeVV/argocd-appset.git/argocd
        targetRevision: '9f3491a85c54c6a0688ef4b5aaad47b77b7c253f'
        path: '{{path}}'
        helm:
          valueFiles:
            - '{{name}}.yaml'


      destination:
        server: '{{server}}' # 'server' field of the secret
        namespace: '{{path[1]}}'
